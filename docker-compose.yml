services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./.env:/app/.env:ro
    restart: unless-stopped

  migrate:
    build: .
    command: alembic upgrade head
    volumes:
      - ./.env:/app/.env:ro
    profiles:
      - migrate
